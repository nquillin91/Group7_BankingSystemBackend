package com.group7.banking.service;

//@Service
//public class BankUserDetailsService implements UserDetailsService {

	/*
	 * @Autowired private UserRepository userDepository;
	 * 
	 * @Override public UserDetails loadUserByUsername(String username) throws
	 * UsernameNotFoundException {
	 * 
	 * UserDetails user = userDepository.find(username);
	 * 
	 * if(user == null){ user = userDaoTableTwo.find(username); }
	 * 
	 * if (user == null) { throw new
	 * UsernameNotFoundException(String.format("Username '%s' not found",
	 * username)); }
	 * 
	 * return user; }
	 */
//}